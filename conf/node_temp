/**
     * {{title}}
     */
    {{funcName}} (params) {
            return this._format({
                url: {{url}},
                {{dataKey}}: {{params}},
                method: '{{method}}'
            })
        }
